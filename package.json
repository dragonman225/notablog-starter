{
  "name": "notablog-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate": "node node_modules/notablog",
    "build": "npm run generate",
    "dev:generate": "node ../notablog",
    "dev:clean": "rm -rf source/notion_cache public",
    "dev:prof": "node --prof ../notablog"
  },
  "dependencies": {
    "notablog": "0.3.7"
  }
}
